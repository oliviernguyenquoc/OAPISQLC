openapi: 3.1.0
info:
  title: Read or Write Only Schema Test
  version: 1.0.0
components:
  schemas:
    user:
      type: object
      properties:
        id:
          type: integer
          format: int64
          readOnly: true
        password:
          type: string
          writeOnly: true
        email:
          type: string
        createdAt:
          type: string
          format: date-time
          readOnly: true
